server:
  port: 9000

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          clone-on-start: true
          uri: https://github.com/developerdhairya/Microservices-Configurations
